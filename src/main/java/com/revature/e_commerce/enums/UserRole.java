package com.revature.e_commerce.enums;

/**
 * Represents the possible roles a user can have in the system.
 *
 * <p>Roles include USER (general user), ADMIN (admin user), and SELLER (business user).</p>
 */
public enum UserRole {
    USER,
    ADMIN,
    SELLER
}

